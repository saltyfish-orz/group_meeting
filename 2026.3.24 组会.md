# 基于双视图 X-ray 的 3D CT 重建

---

## 1. 近期工作

1. **复现并理解 X2CT-GAN**
   - 目标是理解X2CT-GAN的网络结构、训练机制并对其进行复现。
2. **复现 DiffuX2CT**
   - 在没有官方源码的前提下，根据论文和X2CT-GAN重建了一个工程。

---

## 2. X2CT-GAN

### 2.1 论文要解决的问题

> 输入两张正交的 X-ray 图像，直接重建一个 3D CT 体数据。

X2CT-GAN 同时引入：

- **生成器**：把双视图 2D 特征提升为 3D 体数据；
- **条件判别器**：约束结果既像 CT，又与当前 X-ray 条件匹配；
- **投影约束**：让生成 CT 的投影仍然与输入 X-ray 保持一致。

### 2.2 x2ct的实现流程

#### 2.2.1 数据输入

```text
输入
  ct      -> [128, 128, 128]
  xray1   -> [128, 128]
  xray2   -> [128, 128]
加入通道维并组batch后:
  G_real   -> [B, 128, 128, 128]
  G_input1 -> [B, 1, 128, 128]
  G_input2 -> [B, 1, 128, 128]
```

#### 2.2.2 多视图生成器(dense_generator_multiview.py)

- 负责 2D 特征提取、2D 到 3D bridge、3D 解码恢复；

```text
Step 1. 双视图输入
  xray1, xray2 -> [B, 1, 128, 128]

Step 2. 拼接后进入 2D 编码器
  concat -> [B, 2, 128, 128]

Step 3. 2D 编码5步下采样
  block_num = [6, 12, 24, 16, 6], growth_rate=32
  -> [B, 64, 128, 128]
  -> [B, 128, 64, 64]
  -> [B, 256, 32, 32]
  -> [B, 512, 16, 16]
  -> [B, 512, 8, 8]
  -> [B, 704, 1, 1]

Step 4. 2D -> 3D bridge
  平坦化 + 全连接层
  [B, 704, 1, 1] -> [B, 704] -> [B, 256 * 4 * 4 * 4] -> [B, 256, 4, 4, 4]

Step 5. 3D 解码器逐步上采样
  -> [B, 128, 8, 8, 8]
  -> [B, 64, 16, 16, 16]
  -> [B, 32, 32, 32, 32]
  -> [B, 16, 64, 64, 64]
  -> [B, 16, 128, 128, 128]

Step 6. 输出 CT
  G_fake_D -> [B, 1, 128, 128, 128]
  squeeze  -> [B, 128, 128, 128]
```

#### 2.2.3 多尺度判别器 (patch_discriminator.py)

```text
在条件模式下接受 `[xray1_cond, xray2_cond, ct]` 作为输入。
将真实 CT:[B, 1, 128, 128, 128]
两张X光xray_cond ->[B, 1, 128, 128, 128]扩展为3D条件拼接为[B, 3, 128, 128, 128]输入
```

并且把输入继续下采样，送给多个尺度的判别器。

```text
    [B, 3, 128, 128, 128] kernel=4, stride=2, padding=2
 -> [B, 64, 64, 64, 64]
 -> [B, 128, 32, 32, 32]
 -> [B, 256, 16, 16, 16]
 -> [B, 512, 15, 15, 15]  kernel=4, stride=1, padding=1
 -> [B, 1, 14, 14, 14]    打分 out = ⌊(in + 2p - k) / s⌋ + 1
```

#### 2.2.4  投影一致性

除了 GAN 约束，`multiView_CTGAN.py` 里还会对 3D CT 沿多个维度做平均投影：

```text
[B, 1, 128, 128, 128] -> 沿 D/H/W 做平均投影 [B, 1, 128, 128]
```

然后用这些 2D 投影和真实 CT 的投影图做一致性约束。

#### 2.2.5 训练目标

X2CT-GAN 主要由三类损失驱动：

**GAN loss**

- 让生成 CT 的整体分布更像真实 CT

**Identity / Reconstruction loss**

- 默认 MSE，让生成 CT 在体素层面接近真值

**Map projection loss**

- 默认 L1，让 CT 的多视图投影与目标 CT 保持一致

3DGAN训练顺序：

```text
1. 先前向传播生成 G_fake
2. 冻结 D，更新 G
   - GAN loss
   - reconstruction loss
   - projection loss
3. 解冻 D，更新 D
   - real_input 判真
   - fake_input 判假
```

## 3. DiffuX2CT

### 3.1 论文要解决的问题

> 同样的，输入两张正交的 X-ray 图像，直接重建一个 3D CT 体数据。

DiffuX2CT 同时引入

1. **三维解耦生成器**
   - 用三个二维特征平面来表示三维结构；
   - 先把 3D latent 表达拆成三个 2D 平面，再2D平面里做扩散。
2. **交叉卷积**
   - 双视图特征使用交叉卷积，更早利用互补信息
3. **隐性/结构化解码**
   - 恢复出的不是最终 CT，而是一个更结构化的潜空间表示；
   - 再通过三平面解码器生成 3D CT。
4. **投影一致性**
   - 和 X2CT-GAN 一样，DiffuX2CT 也不只依赖体素监督；
   - 还会把预测 CT 重新投影回双视图，约束其与输入 X-ray 一致。

> **先把 3D CT 压缩到一个适合建模的潜空间，再在潜空间里做条件生成。**
>
> **![3f083cbb-3eba-4dd5-9963-595d7237a46e](C:\Users\28354\Desktop\3f083cbb-3eba-4dd5-9963-595d7237a46e.png)**


### 3.2 Diffux2ct的实现流程

#### 3.2.1 数据读入 

```text
单样本:
  ct   -> [1, 128, 128, 128]
  xray -> [2, 256, 256]
  batch = 2
组成 batch 后:
  ct   -> [B, 1, 128, 128, 128]
  xray -> [B, 2, 256, 256]
```

#### 3.2.2 自编码器编码（autoencoder.py 和 triplane.py）

```text
输入 CT:
  [B, 1, 128, 128, 128]

3D encoder:
  -> [B, 32, 128, 128, 128]
  -> [B, 64, 64, 64, 64]
  -> [B, 128, 32, 32, 32]
  -> [B, 32, 16, 16, 16]

Tri-plane projector:
  xy -> [B, 32, 16, 16]
  xz -> [B, 32, 16, 16]
  yz -> [B, 32, 16, 16]

pack:
  z0 -> [B, 96, 16, 16]
```

把原本 `128^3` 的 3D 体数据，压缩成了三个 2D 平面的组合。

#### 3.2.3 条件编码

双视图 X-ray 分别过两个分支，然后融合：

```text
输入xray -> [B, 2, 256, 256] ->  ap  -> [B, 1, 256, 256] ->每路 2D encoder -> [B, 64, 32, 32] 
						  ->  lat -> [B, 1, 256, 256] ->

拼接 + 1*1conv: -> [B, 64, 32, 32]

插值到 latent size: 空间条件特征cond_map -> [B, 64, 16, 16]
全局池化: 全局条件 -> [B, 64]
```

两类条件信息使扩散 U-Net 在每个空间位置都能看到更贴近局部结构的条件提示。

#### 3.2.4 扩散训练

训练时并不是直接生成 CT，而是生成潜变量 `z0`：

```text
真实潜变量 z0 -> [B, 96, 16, 16]

随机采样时间步 t -> [B]

加噪后 x_t -> [B, 96, 16, 16]

扩散 U-Net 输入 concat(x_t, cond_map) -> [B, 160, 16, 16]

预测噪声 noise_pred -> [B, 96, 16, 16]

由 x_t 和 noise_pred 反推 z0_pred -> [B, 96, 16, 16]
```

扩散模型的主干网络在 `diffusion_unet.py` 中定义。

- 在 tri-plane latent 上做 2D 条件去噪
- 接收 `z_t + cond_map + cond_vec + timestep`
- 预测噪声

#### 3.2.5 三平面解码

```text
z0_pred -> unpack
  xy -> [B, 32, 16, 16]
  xz -> [B, 32, 16, 16]
  yz -> [B, 32, 16, 16]

扩展成 3D:
  xy_3d -> [B, 32, 16, 16, 16]
  xz_3d -> [B, 32, 16, 16, 16]
  yz_3d -> [B, 32, 16, 16, 16]

拼接坐标网格:
  coord -> [B, 3, 16, 16, 16]

三平面融合后:
  fused -> 665

3D decoder 输出:
  -> [B, 1, 16, 16, 16]

插值到最终 CT:
  ct_pred -> [B, 1, 128, 128, 128]
```

#### 3.2.6 投影一致性损失

```text
ct_pred -> [B, 1, 128, 128, 128]
AP projection:
  沿 D 做平均 -> [B, 1, 128, 128]
LAT projection:
  沿 W 做平均 -> [B, 1, 128, 128]
拼接:
  proj -> [B, 2, 128, 128]
插值到输入 X-ray 尺寸:
  bip_proj -> [B, 2, 256, 256]
```

然后用这个投影结果和输入的 `xray` 做 L1 loss。

### 3.3 DiffuX2CT 的损失与训练逻辑

当前实现的 diffusion 阶段损失主要包括三部分：

**diffusion loss**（10）

- 噪声预测 MSE
- 保证扩散模型学会正确的去噪方向

**reconstruction loss**（5）

- `ct_pred` 和 `ct_gt` 的 L1
- 保证最终 CT 的体素质量

**projection loss**（1）

- `bip_proj` 和输入 X-ray 的 L1
- 约束几何一致性

### 3.4 训练过程

#### 阶段 A：训练三平面自编码器

- 先学会把真实 CT 压缩成稳定的 latent plane
- 再把 latent plane 解码回 CT

#### 阶段 B：训练条件扩散模型

- 让扩散模型在双视图 X-ray 条件下，从高斯噪声逐步恢复到正确的潜变量；
- 再通过解码器恢复 CT。


### 3.5 对 DiffuX2CT 的当前理解

> DiffuX2CT 通过“先压缩、再扩散、再解码”的分治策略，把原本极难的 2D 到 3D 生成问题，转化为一个更稳定的潜空间条件生成问题。

它相比 X2CT-GAN 的主要变化是：

- 从直接回归转向逐步去噪；
- 从 3D voxel 主建模转向 tri-plane latent 主建模；
- 从 GAN 转向 diffusion 的噪声预测，但仍然保留 projection consistency 这一类观测空间约束。

