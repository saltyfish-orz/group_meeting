# 人脸面部关键点检测组会汇报

## 1. 研究背景

人脸面部关键点检测是计算机视觉中的基础任务之一，目标是在输入图像或视频中定位具有明确语义的人脸标志点，例如眼角、鼻尖、嘴角、下巴点等。该任务广泛应用于人脸识别、表情分析、身份认证、疲劳检测、AR 交互、医学测量与颌面分析等场景。

## 2. 任务定义

### 2.1 二维关键点检测

给定一张人脸图像，预测若干个关键点在图像平面中的坐标：

$$
\mathcal{L}_{2D} = \{(u_i, v_i)\}_{i=1}^{N}
$$

其中，N 表示关键点数量，常见设置为 5/6 点<img src="C:\Users\28354\AppData\Roaming\Typora\typora-user-images\image-20260407180905121.png" alt="image-20260407180905121" style="zoom:25%;" />、68 点<img src="C:\Users\28354\AppData\Roaming\Typora\typora-user-images\image-20260407181110232.png" alt="image-20260407181110232" style="zoom: 33%;" />、98 点<img src="C:\Users\28354\AppData\Roaming\Typora\typora-user-images\image-20260407180125127.png" alt="image-20260407180125127" style="zoom:15%;" />等。

### 2.2 三维关键点检测

在二维关键点基础上，进一步估计每个关键点在三维空间中的坐标：

$$
\mathcal{L}_{3D} = \{(x_i, y_i, z_i)\}_{i=1}^{N}
$$

对于单张 RGB 图像，深度信息并不直接可观，因此通常需要借助三维人脸先验模型完成三维恢复。

## 3. 研究意义

### 3.1 通用视觉领域

- 提升人脸对齐、识别与表情分析精度
- 支持人机交互和增强现实应用
- 为三维人脸建模和动画生成提供基础

### 3.2 医学相关领域

对于医学场景，还要满足以下要求：

- 具有明确解剖学意义
- 在重复拍摄中具有稳定性
- 对姿态、光照和表情变化具有鲁棒性
- 可以支持后续距离、角度和对称性分析

## 4. 技术发展脉络

### 4.1 传统方法

早期方法主要包括：

- 主动形状模型, ASM
- 主动外观模型, AAM
- 级联回归

特点：

- 依赖人工设计特征
- 对姿态和遮挡鲁棒性有限
- 在受控环境中表现尚可

### 4.2 深度学习二维关键点检测

随着卷积神经网络的发展，二维关键点检测主要采用两类方式：

- 坐标回归
- 热力图回归

代表方法包括：

- 人脸对齐网络FAN
- HRNet 系列
- 基于 Transformer 的 face alignment 方法

### 4.3 三维关键点检测与单目三维恢复

由于单张 RGB 图像没有直接深度，因此当前主流方法通常不是“直接把二维点投到三维”，而是采用以下思路：

1. 检测二维关键点
2. 引入三维人脸先验模型
3. 拟合三维形状、姿态与相机参数
4. 得到关键点的三维坐标

这类方法的核心是：

**二维观测 + 三维先验 -> 单目三维恢复**

## 5. 当前主流技术路线

### 5.1 二维关键点检测 + 三维人脸模型拟合

流程：

输入图像 -> 检测二维关键点 -> 拟合 3DMM/FLAME -> 输出三维关键点

优点：

- 适合单张图像输入
- 可输出具有一致语义的三维点
- 可解释性较强
- 更适合医学分析

缺点：

- 依赖三维先验模型
- 恢复结果受模型表达能力限制
- 绝对尺度恢复通常不可靠

### 5.2 端到端三维关键点回归

流程：

输入图像 -> 网络直接输出三维坐标

优点：

- 推理简单
- 端到端训练方便

缺点：

- 需要较强的三维标注支持
- 可解释性较弱
- 泛化能力受训练数据影响较大

### 5.3 先重建三维人脸，再提取稀疏关键点

流程：

输入图像 -> 重建三维人脸网格 -> 从固定模板提取关键点

优点：

- 能提供更丰富的几何信息
- 除关键点外，还可做区域形态分析

缺点：

- 建模复杂
- 对数据和算力要求更高
- 对当前仅需几十个点位的任务而言，可能偏重

## 6. 核心模型与先验

### 6.1 三维可形变人脸模型, 3DMM

3DMM 是三维人脸分析中的经典模型，将人脸形状表示为若干基向量的线性组合：
$$
S = \bar{S} + A_{id}\alpha + A_{exp}\beta
$$

其中：

- S_bar 为平均人脸形状
- A_id 为身份形变基
- A_exp 为表情形变基
- α，β 为对应系数

意义：

- 可解释性强
- 适合拟合单张图像
- 可将关键点定义在统一模板上

### 6.2 FLAME 模型

FLAME 是近年来常用的人脸参数化模型，能够同时建模：

- 身份形状
- 表情变化
- 头部姿态

其优势在于：

- 模板拓扑固定
- 适合从模板读取固定语义点
- 更适合后续分析、统计与比较

## 7. 代表性工作

| 类别 | 代表工作 | 核心思想 | 问题与启发 |
| --- | --- | --- | --- |
| 三维人脸模型基础 | 3D Morphable Model | 3D 结构可以由很多独立控制人脸特征进行加权相加/相乘 | 对初始条件敏感、环境干扰会影响精度 |
| 参数化模板模型 | FLAME | 用统一模板建模身份、表情和姿态 | 模型先验、表达能力受模板能力限制 |
| 单图三维人脸对齐 | 3DDFA | 从单张图像估计三维形状与姿态、从二维点回归推进到基于三维模型的对齐 ||
| 三维头脸数据集 | DAD-3DHeads | 提供更强的三维头脸基准 |形状 + 姿态 + 点位联合监督|
| 半监督三维关键点定位 | FaceLift | 3D模板存在差异，所以直接利用二维标注学习三维关键点 | 依赖2D检测器和3DGAN |
|  |  |  |  |

## 8. 常用数据集与标注资源

### 8.1 二维关键点数据集

- 300W、AFW、HELEN、LFPW、IBUG（68点）
- AFLW、COFW（21或29点）

特点：

- 标注成熟
- 适合二维关键点检测训练
- 对遮挡、姿态、表情变化已有较多覆盖

### 8.2 三维关键点或三维形状相关数据

- AFLW2000-3D
- 300W-LP
- BIWI
- Florence
- DAD-3DHeads

特点：

- 可用于三维人脸对齐或三维关键点恢复
- 部分数据带有姿态变化和深度信息

## 9. 评价指标

### 9.1 二维关键点检测指标

- 归一化平均误差
- AUCa
- 累积误差分布曲线

其中，NME 常用于评价预测点与真实点之间的平均偏差，并用眼间距、瞳距或包围框大小做归一化。

### 9.2 三维关键点检测指标

- 三维点欧氏距离误差
- 平均3D路标误差

### 9.3 医学场景建议增加的指标

- 点间距离误差、角度误差、区域误差
- 重复拍摄一致性
- 标注者间一致性

## 10. 现有方法面临的主要问题

### 10.1 单张图像三维恢复存在先天不适定性

单张 RGB 图像缺乏直接深度信息，因此三维结果通常依赖先验模型。恢复出的结果往往更接近“合理的三维人脸估计”，而不一定是真实的绝对三维形状。

### 10.2 关键点定义不统一

不同数据集、不同论文使用的关键点协议并不一致，例如：

- 点的数量不同
- 点的位置定义不同
- 轮廓点和软组织点的界定不同

这会影响模型训练、评测和结果比较。

### 10.3 数据集与真实场景之间存在域差异

公开数据集中的拍摄条件、姿态和标注方式，与医学实际场景可能存在明显差异，因此模型泛化能力是一个关键问题。

