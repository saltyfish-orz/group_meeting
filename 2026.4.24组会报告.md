# SAX-NeRF 论文组会报告

论文：**结构感知的稀疏视角 X-ray 三维重建（Structure-Aware Sparse-View X-ray 3D Reconstruction）**  
会议：CVPR 2024  
作者：Cai 等  
论文链接：[CVPR 开放获取页面](https://openaccess.thecvf.com/content/CVPR2024/html/Cai_Structure-Aware_Sparse-View_X-ray_3D_Reconstruction_CVPR_2024_paper.html)  
源码链接：[GitHub: caiyuanhao1998/SAX-NeRF](https://github.com/caiyuanhao1998/SAX-NeRF)



---

## 1. 论文研究背景

X-ray 成像具有较强穿透能力，可以揭示物体内部结构，因此被广泛用于医学成像、生物观察、安全检查和工业检测等场景。与普通可见光成像不同，X-ray 图像不是物体表面反射形成的图像，而是射线穿过物体后发生衰减形成的投影结果。

传统 CT 或 CBCT 重建通常需要从多个角度采集大量 X-ray 投影。投影数量越多，三维重建越稳定，但同时也会带来更高的辐射剂量和采集成本。因此，如何在稀疏投影视角下完成高质量三维重建，是低剂量成像和快速三维成像中的重要问题。

近年来，NeRF 类隐式神经场方法被引入 X-ray 三维重建。其优势在于不一定需要成对 CT 体数据作为监督，而是可以通过投影一致性优化一个三维连续场。然而，作者指出，已有基于 NeRF 的 X-ray 方法仍存在明显不足：它们大多直接使用普通多层感知机（MLP）来表示三维场，没有充分考虑 X-ray 成像中“射线穿透复杂内部结构”的特点。

---

## 2. 论文要解决的问题

论文要解决的核心问题是：

> 在稀疏 X-ray 投影条件下，如何设计一种结构感知的神经场方法，使其能够更好捕捉 X-ray 穿透路径上的复杂内部结构，并利用投影图像中的局部和全局结构信息，从而提升新视角合成和 CT 重建质量。

作者认为已有方法主要有两个技术缺陷：

1. **3D 内部结构建模不足**  
   普通多层感知机（MLP）对一条 X-ray 射线上的采样点基本逐点处理，难以捕捉不同采样点之间的结构依赖。

2. **2D 投影结构利用不足**  
   普通随机射线采样会采到大量背景或无效区域，也难以利用投影图像中的局部上下文与全局几何结构。

因此，SAX-NeRF 的核心思路就是同时建模两类结构：

1. 射线路径上的 **三维内部结构依赖**。
2. 投影平面上的 **二维局部-全局结构信息**。

---

## 3. 方法概述

论文的方法概述图可以分成三个部分：

1. **(a): SAX-NeRF 整体流程**
2. **(b): 基于线段的注意力块，LSAB**
3. **(c): 基于线段的多头自注意力, LS-MSA**

![image-20260423103926919](C:\Users\28354\AppData\Roaming\Typora\typora-user-images\image-20260423103926919.png)

### 3.1 SAX-NeRF 整体流程

(a) 展示了 SAX-NeRF 的完整数据流。它不是输入 X-ray 直接输出 CT 的前馈网络，而是一个 **基于投影一致性的隐式场优化框架**。

整体流程可以拆成六步。

**第一步：输入稀疏 X-ray 投影**

输入是一组稀疏角度采集到的 X-ray 投影。论文设定的是 CBCT，即扫描器发出锥形 X 光，并在等角度间隔下采集投影。

**第二步：使用 MLG 策略采样射线**

模型不会直接使用整张投影图训练，而是从投影图中采样一批 X-ray 射线，记作 `R`。这里使用的不是普通随机采样，而是论文提出的 **掩码局部-全局射线采样**。

MLG 的作用是：

1. 用掩膜排除背景无效区域。
2. 用小区域采样提取局部上下文。
3. 用像素级采样保留全局几何。

**第三步：在每条射线上采样 N 个三维点**

对于训练批次中每条 X-ray 射线 `r ∈ R`，沿射线路径均匀采样 `N` 个三维点：

`P = {p1, p2, ..., pN}`

每个点都是三维空间中的一个坐标位置。

**第四步：将采样点送入 Lineformer**

这些点首先经过哈希编码，得到点特征，然后输入 Lineformer，在一条 X-ray 射线上的点序列上做注意力建模。  
Lineformer 是 SAX-NeRF 的主干网络，用于预测每个采样点的放射密度。

**第五步：X-ray 体渲染并计算投影损失**

得到一条射线上所有点的放射密度后，模型根据 X-ray 衰减规律进行体渲染，得到预测投影强度。然后将预测投影强度与真实投影像素值进行比较，通过投影重建误差反向优化整个神经场。

可以简单理解为：

> 模型不断调整三维放射密度场，使它从任意训练角度渲染出来的 X-ray 投影尽可能接近真实投影。

---

## 4. 核心模块一：MLG 射线采样

MLG 是掩码局部-全局射线采样，对应 (a) 左侧的采样模块。

### 4.1 MLG要解决的问题

普通 NeRF 方法通常随机采样像素射线。但在 X-ray 投影中，这样做有两个问题：

1. 投影图中存在大量背景区域，随机采样容易采到无信息射线。
2. 单点随机采样不能充分利用邻近像素之间的局部上下文。

因此，作者提出 MLG，用更有效的方式采样训练射线。

### 4.2 MLG 实现方法

MLG 包含三步。

**第一步：生成前景掩膜**

作者对 X-ray 投影进行阈值处理，得到前景掩膜。作用是区分有信息的物体区域和背景区域。

**第二步：局部小区域采样**

在前景区域中裁剪若干局部小区域，并采样区域内像素对应的射线。  
这部分采样的目的是帮助模型学习局部结构和上下文信息。

**第三步：像素级抽样**

在前景区域的其他位置随机采样单个像素对应的射线。  
这部分采样的目的是帮助模型学习整体形状和全局几何。

### 4.3 MLG 的意义

MLG 的意义在于：

1. 避免无效背景采样。
2. 让训练批次中包含局部连续结构。
3. 同时保留全局随机采样，避免模型只关注局部区域。

所以，MLG 不是简单的数据增强，它负责从二维投影平面提取局部和全局结构线索。

---

## 5. 核心模块二：Lineformer

Lineformer 是 SAX-NeRF 的主干网络。

传统 NeRF 通常使用多层感知机（MLP）表示隐式场。对于一条射线上的多个采样点，MLP 往往是逐点处理的。  
但在 X-ray 成像中，一条射线会穿过不同组织或材料，最终投影强度是整条路径上衰减的累积结果。因此，射线上的点之间存在结构依赖。

作者认为：

> X-ray 在穿透不同结构内容时会产生不同程度的衰减。  
> 普通多层感知机（MLP）难以建模这些内部结构关系。

### Lineformer 的基本思路

Lineformer 把一条射线上的采样点看成一个有序点序列，然后进行线段级注意力建模。

流程如下：


论文中 Lineformer 的输入是：`P ∈ R^{N×3}`

也就是一条射线上的 `N` 个三维坐标点。

经过哈希编码后得到：`F ∈ R^{N×C}`

然后通过多个 LSAB，最后输出：`D ∈ R^N`

也就是每个点的放射密度。

---

## 6. LSAB 的结构

LSAB 是 **基于线段的注意力块（Line Segment-based Attention Block）**，是 Lineformer 的基本单元。

(b) 展示了一个 LSAB 内部结构。它主要由以下部分组成：

全连接层 -> 层归一化 -> LS-MSA -> 跳跃连接 ->前馈网络 -> 跳跃连接

结构上类似 Transformer 模块，但最关键的区别是：  
它的注意力不是普通全局注意力，而是 **基于线段的多头自注意力（Line Segment-based Multi-head Self-Attention）**。

可以这样理解：

> LSAB 是一个专门为 X-ray 射线点序列设计的 Transformer 模块。

---

## 8. LS-MSA 的结构

LS-MSA 是 **基于线段的多头自注意力（Line Segment-based Multi-head Self-Attention）**，是 Lineformer 中最核心的模块。

### 8.1 LS-MSA的区别

普通自注意力会在所有 `N` 个采样点之间计算两两关系，计算复杂度是 `O(N^2)`。  
但 SAX-NeRF 将一条射线上的 `N` 个点划分成 `M` 个线段，每个线段中有 `N/M` 个点。

然后：

1. 只在线段内部做自注意力。
2. 不在整条射线上做全局注意力。
3. 最后再把所有线段的结果拼接起来。

### 8.2 设计思路

原因有两个。

**第一，更符合 X-ray 内部结构**

一条 X-ray 穿过人体或物体时，不同路径段可能对应不同结构区域。  
例如在医学场景中，一条射线可能依次穿过皮肤、骨骼、肺组织、血管或其他结构。线段级注意力可以在局部路径范围内建模内部依赖。

**第二，计算更高效**

普通全局注意力复杂度与 `N^2` 成正比，而 LS-MSA 将注意力限制在线段内部，使复杂度接近与 `N` 线性相关。论文特别强调，这使得 Transformer 能够更实际地用于 X-ray 神经渲染。

### 8.3 LS-MSA 的直观理解

可以把 LS-MSA 理解成：

> 将一条 X-ray 射线切成若干小段，每一段内部判断这些采样点之间的结构关系，然后综合所有线段得到整条射线的衰减分布。

这也是 SAX-NeRF 中“结构感知（Structure-Aware）”最核心的体现。

---

## 9. 方法图中的训练目标

SAX-NeRF 的训练不是 CT 监督，而是投影监督。

模型训练的核心逻辑是：

1. 当前神经场预测每个采样点的放射密度。
2. 沿射线对放射密度做体渲染，得到预测投影值。
3. 预测投影值与真实投影值计算误差。
4. 通过误差反向传播更新神经场。

简化公式可以写成：

`损失函数 = || I_pred(r) - I_gt(r) ||^2`

其中：

1. `I_pred(r)` 是模型渲染出的投影值。
2. `I_gt(r)` 是真实 X-ray 投影值。

这个训练目标的优点是：

1. 不需要真实 CT 体作为监督。
2. 和 X-ray 投影物理一致。
3. 可用于新视角合成和 CT 重建。

---

## 10. 实验结果摘要

论文在 X3D 基准数据集上验证 SAX-NeRF，任务包括：

1. 新视角合成
2. CT 重建

新视角合成

SAX-NeRF 在 NVS 任务上取得最优结果：

1. 平均 PSNR：`51.37`
2. 平均 SSIM：`0.9994`

相比 NAF，平均 PSNR 提升约 `12.56 dB`。



CT 重建

SAX-NeRF 在 CT 重建任务上同样取得最优结果：

1. 平均 PSNR：`37.25`
2. 平均 SSIM：`0.9753`

相比 NAF，平均 PSNR 提升约 `2.49 dB`。
